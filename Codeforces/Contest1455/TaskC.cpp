#include <bits/stdc++.h>

using namespace std;
#define int long long

signed main() {
	ios_base::sync_with_stdio(0); cin.tie(0);
	freopen("TaskC.INP", "r", stdin);

	int tc;
	cin >> tc;
	while (tc--) {
		int x, y;
		cin >> x >> y;
		cout << x - 1 << ' ' << y << endl;
	}

	return 0;
}